{
  "name": "capacitor-manifest-updater",
  "version": "0.0.2",
  "description": "This package is used to update the AndroidManifest.xml file for Capacitor projects",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "pretest": "npm run build",
    "test": "vitest",
    "prepublish": "npm run build",
    "publish": "npm publish --access public"
  },
  "bin": {
    "update-manifest": "./updateManifest.js"
  },
  "repository": {
    "url": "https://github.com/yoav0gal/capacitor-manifest-updater"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.6",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "execa": "^8.0.1",
    "vitest": "^1.2.1"
  }
}
